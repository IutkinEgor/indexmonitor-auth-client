/*
 * INTERFACE TEMPLATE
 * Use this template to create own interface. Import your interface to SharedModuel through _index.ts
*/

export interface TmpInterface{
    id: string
    name: string
    price: number
}